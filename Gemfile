source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gemspec

# Local dependency for testing integration (temporarily disabled for release)
# gem "vivlio-starter", path: "../vivlio-starter"
